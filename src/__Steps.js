// Steps for integrated firebase with App;
/*
1.Create a project into firebase(My-Practice-App);
2.Register App and Check mark for firebase hosting;
3.npm install firebase(yarn add firebase);
4.config firebase into firebase.init.js and const auth=getAuth(app) and export default auth and 
    also import {getAuth} from "firebase/auth";
5.Authentication and choose desire provider(like email/password,google etc);
6.
 */

//firebase hosting steps;

/*
1.npm install -g firebase-tools;
2.firebase login;
3.firebase init;
4.npm build;
5.firebase deploy;
 */

// react-router setup steps;

/*
1.yarn add react-router-dom@6 (install each project);
2.import { BrowserRouter } from "react-router-dom" into src/index.js and wrap App by it;
3.open src/App.js and import { Routes, Route} from "react-router-dom";
4.
 */
// Activites of hole project;

/*
1.Create a Pages/Home/Login/About/Shared(Header(Header.js/Header.css),Footer(Footer.js/Footer.css));
folder into src;
2.Attatch <Header></Header> componet top of Routes and attatch <Footer></Footer>
    componet under  of all Routes;
3. Into Home pages create Home.js and Home.css, Services.js & Services.css component;
4.
 */
